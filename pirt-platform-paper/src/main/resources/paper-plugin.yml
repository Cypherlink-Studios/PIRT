name: PIRT
version: "${version}"
main: dev.darkblade.pirt.platform.paper.PirtPlugin
api-version: "1.20"
author: DarkBlade
description: Players In Region Tracker - Decoupled high-performance region queries
dependencies:
  server:
    WorldGuard:
      load: BEFORE
      required: false
    PlaceholderAPI:
      load: BEFORE
      required: false
